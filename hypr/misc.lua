-- ----------------------------------------------------- 
-- Misc settings
-- ----------------------------------------------------- 


hl.config({
    misc = {
        disable_hyprland_logo = true,
        disable_splash_rendering = true,
        -- Let a replacement lockscreen take over if the current one dies
        -- (otherwise the session stays locked and needs a reboot).
        allow_session_lock_restore = true,
    },
})

